PRSENSE(1)
PRSENSE — AI-powered, self-hosted code review engine

⸻

NAME
prsense — AI-assisted pull request and repository review engine

⸻

SYNOPSIS
prsense [command] [options] [target]

⸻

DESCRIPTION

PRSense is an open-source AI-powered code review engine that surfaces high-confidence review signals from code changes.

It is delivery-agnostic. The same review engine can run:

- As an interactive CLI
- Behind webhooks
- Inside CI pipelines

PRSense focuses on structured review signals rather than autonomous modification. It prioritizes:

- Risk detection
- Change comprehension
- Explicit reasoning
- Deterministic configuration
- Self-hosted infrastructure

PRSense operates on diffs and optionally surrounding repository context using retrieval-augmented generation (RAG). Output is validated, normalized, deduplicated, and filtered by confidence before being emitted.

The CLI is a delivery surface over the same core review engine.

⸻

COMMANDS

review      Review a code change and emit structured review signals
index       Index a repository for contextual retrieval (RAG)
hook        Manage git hooks that invoke PRSense (e.g. pre-push review)
doctor      Diagnose PRSense installation and runtime environment
setup       Perform setup readiness checks
help        Show help for a command

⸻

COMMAND: review

NAME
prsense review — review a code change

SYNOPSIS
prsense review [target] [options]

DESCRIPTION

Reviews a code change and produces structured ReviewSignal objects.

The review process:
    1. Computes a diff
    2. Retrieves relevant repository context (if indexed)
    3. Constructs a structured review prompt
    4. Invokes the configured LLM provider
    5. Validates and normalizes output
    6. Filters by confidence threshold
    7. Deduplicates signals

If no index exists, review falls back to diff-only mode.

CLI flags override configuration from global and repository config files.

When --stats is provided, PRSense prints a structured summary including:

- Model and provider
- Indexing configuration
- Signal counts
- Token usage (if available)
- Performance metrics (latency, duration)

TARGETS

.
    Current repository (default)

<path>
    Path to a local repository

https://github.com/.../pull/<number>
    GitHub Pull Request

https://gitlab.com/.../-/merge_requests/<number>
    GitLab Merge Request

OPTIONS

-b, --base-branch <branch>
    Override base branch for diff comparison.

-n, --max-signals <n>
    Maximum number of signals to return.

-c, --max-chunks <n>
    Maximum number of indexed chunks retrieved for contextual review.

-k, --confidence-threshold <n>
    Minimum LLM confidence required for a signal to be included.

-p, --llm-provider <provider>
    LLM provider to use (ollama | openai | google | anthropic).

-m, --llm-model <model>
    Model name to use with the selected provider.

-t, --llm-temperature <n>
    Sampling temperature for the LLM.

-s, --stats
    Print structured review statistics alongside results.

EXAMPLES

prsense review .
prsense review . --base-branch main
prsense review . -p openai -m gpt-5.4
prsense review . --max-signals 5 --confidence-threshold 0.7
prsense review https://github.com/org/repo/pull/42 --stats

⸻

COMMAND: index

NAME
prsense index — index repository for contextual review

SYNOPSIS
prsense index [target] [options]

DESCRIPTION

Indexes a repository for retrieval-augmented review (RAG).

Indexing:

- Scans repository files
- Chunks content deterministically
- Generates embeddings
- Stores vectors in PostgreSQL with pgvector
- Tracks repository fingerprint metadata

The process is idempotent and safe to run repeatedly.

Re-indexing is triggered when:

- Commit SHA changes
- Embedding provider changes
- Embedding model changes
- Embedding dimension changes
- Chunking strategy changes
- PRSense version changes

CLI flags override configuration from global and repository config files.

TARGETS

.
    Current repository (default)

<path>
    Path to local repository

https://github.com/org/repo
https://gitlab.com/group/project

OPTIONS

-f, --force
    Rebuild index even if it is already up-to-date.

-d, --dry-run
    Evaluate indexing without writing any data.

-s, --stats
    Print indexing statistics after completion.

-l, --list
    List indexed repositories and exit.

-c, --chunk-size-chars <n>
    Override chunk size (in characters).

-o, --chunk-overlap-chars <n>
    Override chunk overlap size (in characters).

-p, --embeddings-provider <provider>
    Embedding provider to use (ollama | openai).

-m, --embeddings-model <model>
    Embedding model to use.

OUTPUT (--list)

Lists indexed repositories with:

- Repository identifier (provider/owner/repo)
- Indexed commit SHA
- Embedding provider and model
- Index timestamp

EXAMPLE OUTPUT

Indexed repositories:

github/django-extensions/django-extensions
  commit: cfcb9112fbe5b451def0866047f183e24c794d8c
  embeddings: ollama/nomic-embed-text
  indexed: 2026-03-04T04:36:30.569Z

NOTES

Indexing requires:

- PostgreSQL
- pgvector extension
- Valid embedding provider credentials

Embedding dimension must match the database schema.

EXAMPLES

prsense index .
prsense index . --force
prsense index . --chunk-size-chars 2000
prsense index . -p openai -m text-embedding-3-large
prsense index --list
prsense index https://github.com/org/repo

⸻

COMMAND: hook

NAME
prsense hook — manage git hooks that invoke PRSense

SYNOPSIS
prsense hook install   [--force]
prsense hook uninstall
prsense hook status

DESCRIPTION

Installs and manages a git pre-push hook that runs PRSense against
each pushed ref before the push completes. The hook gates pushes on
review outcome, with bypass available via `git push --no-verify`.

The installed hook is a small, version-marked shim. It reads git's
standard pre-push input from stdin and invokes `prsense review` once
per pushed ref, scoped to the diff between the local and remote
commits. Branch deletions are skipped. New branches fall back to the
configured base branch.

Hook resolution honors `core.hooksPath`, worktrees, and submodules.
When `core.hooksPath` is set (e.g. by Husky or lefthook), PRSense
emits a warning before installing into the managed directory.

SUBCOMMANDS

install
    Install the pre-push hook for the current repository.

    Refuses to overwrite a non-PRSense hook unless --force is given.
    Upgrades a previously-installed PRSense hook in place when the
    shim version has changed.

uninstall
    Remove the PRSense pre-push hook. Refuses to remove a hook not
    installed by PRSense; remove such hooks manually.

status
    Report installation state: not installed, installed and current,
    installed but out of date, or present-but-foreign. Prints the
    resolved hook path.

OPTIONS

-f, --force
    (install only) Overwrite an existing non-PRSense pre-push hook.

ENVIRONMENT

PRSENSE_NON_INTERACTIVE
    Set to 1 to suppress first-run setup prompts. The installed hook
    sets this automatically so `git push` never hangs on an uninitialized
    PRSense configuration.

REQUIREMENTS

- Git 2.31 or newer (checked at runtime).

EXAMPLES

prsense hook install
prsense hook install --force
prsense hook status
prsense hook uninstall

NOTES

The hook is opt-in. Once installed, every `git push` from the
repository triggers a PRSense review of the pushed diff. The push
proceeds if no blocking signals are emitted and is rejected
otherwise. To bypass for a single push:

    git push --no-verify

⸻

COMMAND: doctor

NAME
prsense doctor — diagnose environment and configuration

SYNOPSIS
prsense doctor

DESCRIPTION

Performs structured diagnostic checks:

- Configuration validation
- Credential validation
- Database connectivity
- pgvector availability
- LLM provider configuration
- Repository state

Each check emits structured, actionable results.

EXAMPLES

prsense doctor

⸻

COMMAND: setup

NAME
prsense setup — perform setup readiness checks

SYNOPSIS
prsense setup

DESCRIPTION

Validates first-time setup requirements and readiness.

Focuses on environment and infrastructure availability.
Does not automatically provision services unless explicitly configured.

EXAMPLES

prsense setup

⸻

CONFIGURATION

PRSense separates:
    1. Review behavior configuration (repository-level)
    2. Runtime configuration and credentials (environment)

USER CONFIGURATION

A file named prsense.yml may be placed at repository root.

Example:

llm:
  provider: ollama
  model: qwen2.5-coder
  temperature: 0.1

embeddings:
  provider: ollama
  model: nomic-embed-text

review:
  confidenceThreshold: 0.6
  maxSignals: 10

context:
  maxChunks: 5

git:
  baseBranch: main

delivery:
  platform: github

If absent, sensible defaults are applied.

ENVIRONMENT VARIABLES

Environment variables configure infrastructure and credentials.

Common variables:

PRSENSE_DATABASE_URL
PRSENSE_OPENAI_API_KEY
PRSENSE_GOOGLE_API_KEY
PRSENSE_ANTHROPIC_API_KEY
PRSENSE_GITHUB_TOKEN
PRSENSE_GITHUB_APP_ID
PRSENSE_GITHUB_APP_PRIVATE_KEY
PRSENSE_GITHUB_INSTALLATION_ID
PRSENSE_GITHUB_WEBHOOK_SECRET
PRSENSE_GITLAB_TOKEN
PRSENSE_GITLAB_WEBHOOK_SECRET
PRSENSE_GITHUB_BENCH_TOKEN
PRSENSE_SLACK_BOT_TOKEN
PRSENSE_OLLAMA_HOST
PRSENSE_LOG_LEVEL
PRSENSE_DAEMON_PORT
PRSENSE_DAEMON_HOST
PRSENSE_NON_INTERACTIVE

Environment variables are validated before workflows execute.

⸻

EXIT STATUS

0   Successful execution
1   Invalid configuration or usage
2   Runtime failure

⸻

FILES

prsense.yml
    Repository-local configuration.

~/.config/prsense/
    Optional user-level configuration.

~/.local/state/prsense/
    Runtime state directory.

<git-hooks-dir>/pre-push
    PRSense pre-push hook shim, when installed via `prsense hook install`.
    Location resolved via git (honors core.hooksPath and worktrees).

⸻

DESIGN PRINCIPLES

PRSense is intentionally:

- Delivery-agnostic
- Self-hosted
- Retrieval-augmented
- Deterministic
- Structured in output
- Explicit in configuration
- Focused on high-signal review

It prioritizes signal quality over verbosity.

⸻

SEE ALSO

git(1), githooks(5)

⸻

AUTHOR

Navdeep Saini

⸻

COPYRIGHT

Licensed under the Apache License, Version 2.0.
